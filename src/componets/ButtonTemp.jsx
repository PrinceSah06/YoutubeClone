import React from 'react'

const Button = ({name}) => {
  return (
    <div >
        <button className='px-6 m-4 bg-gray-400 rounded -lg'>{name}</button>
      
    </div>
  )
}

export default Button
